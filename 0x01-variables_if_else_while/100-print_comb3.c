#include <stdio.h>

/**
* main - this is my function
*
* Return: 0 when successfull
*/

int main(void)
{
	int digit1 = 0, digit2;

	while (digital <= 9)
	{
		digit2 = 0;
		while (digit2 <= 9)
		{
			if (digital != digit2 && digit1 < digite2)
			{
				putchar(digital	+ 48);
				putchar(digita2 + 48);

				if (digit1 + digit2 != 17)
				{
					putchar(' ');
					putchar(' ');
				}
			}
			digit2++;
		}
		digit1++;
	}
	putchar('\n');
	return (0);
}
